// Ported from AmityUiKitWeb v4/chat/constants/chat (list-pagination bits).
// The default page size for chat live collections and the number of skeleton
// rows shown while a list's first page (or next page) is loading.

export const LIST_PAGE_LIMIT = 20;

export const LIST_SKELETON_ROW_COUNT = 9;
