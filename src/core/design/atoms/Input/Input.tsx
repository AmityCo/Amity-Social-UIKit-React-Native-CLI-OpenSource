// Input atom — compound namespace mirroring AmityUiKitWeb
// core/design/atoms/Input (Input.Text / Input.Boxed / Input.Chip / Input.User).

import { Boxed } from './Boxed';
import { Chip } from './Chip';
import { Text } from './Text';
import { User } from './User';

export const Input = {
  Boxed,
  Text,
  Chip,
  User,
};
