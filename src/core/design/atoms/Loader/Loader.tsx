// Loader atom — compound namespace mirroring AmityUiKitWeb
// core/design/atoms/Loader (Loader.Spinner / Loader.Upload).

import { Spinner } from './Spinner';
import { Upload } from './Upload';

export const Loader = {
  Spinner,
  Upload,
};
